{
  "name": "totum",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css",
    "watch-css": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch",
    "serve": "live-server src --port=8080 --open=/index.html",
    "dev": "npm run build-css && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "tailwind-hamburgers": "^1.3.5",
    "tailwindcss": "^4.1.12"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
